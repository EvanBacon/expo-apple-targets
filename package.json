{
  "name": "@bacons/apple-targets-repo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "expo-yarn-workspaces check-workspace-dependencies",
    "start": "lerna run build --parallel -- --watch",
    "prepare": "lerna run prepare --parallel"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "resolutions": {
    "expo": "^51.0.37",
    "fs-extra": "^9.1.0"
  },
  "devDependencies": {
    "eslint": "^8.10.0",
    "expo-yarn-workspaces": "^2.0.2",
    "lerna": "3.4.1",
    "prettier": "^2.2.1",
    "ts-node": "^10.0.0",
    "typescript": "^4.2.4",
    "expo-module-scripts": "^2.0.0",
    "@types/jest": "^26",
    "jest": "^26.6.3"
  }
}
